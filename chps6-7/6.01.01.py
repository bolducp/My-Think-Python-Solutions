"""Write a compare function that returns 1 if x > y, 0 if x == y, and -1 if x < y."""


def x_y_comparison(x, y):
    if x > y:
        return 1
    elif x == y:
        return 0
    else:
        return -1
